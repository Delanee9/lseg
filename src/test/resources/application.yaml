spring:
  application:
    name: lseg

server:
  port: 8081
  servlet:
    context-path: /api
  error:
    include-message: always

lseg:
  input:
    path: ./inputData
  output:
    path: ./outputData

springdoc:
  api-docs:
    path: /docs